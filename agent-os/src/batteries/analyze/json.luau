-- Typed stub of AgentOS require("json") for luau-analyze only.
-- Runtime uses the embedded json builtin (this file is not on the execute path).

local json = {}

function json.encode(_value: any): string
	return "{}"
end

function json.decode(_text: string): any
	return nil
end

return json

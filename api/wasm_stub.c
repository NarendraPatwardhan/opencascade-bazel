/* Empty translation unit for the Wasm link of //api:libocc_c_wasm_bin.
 *
 * Emscripten produces a modular JS+Wasm library with --no-entry (no main).
 * All public symbols come from occ_c via EXPORTED_FUNCTIONS on the binary.
 */

bazel-*
OCCT
OCP
build123d

# Local design notes / exploration only (not the shipped product tree).
# AgentOS product code lives under agent-os/ (BSL); Apache kernel stays at repo root.
scripting/

# Local AgentOS release + staged demo trees (downloaded / generated).
agent-os/vendor/
agent-os/_stage/

# Packed release artifacts (produced by scripts/pack-demo-stage.sh).
dist/
node_modules/
